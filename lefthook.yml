pre-commit:
  parallel: true
  commands:
    lint:
      run: pnpm -r run lint
    format:
      run: pnpm --filter ./apps/server run format:check

pre-push:
  commands:
    tests:
      run: pnpm --filter ./apps/server run test:coverage
