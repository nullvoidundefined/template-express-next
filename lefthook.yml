pre-commit:
  parallel: true
  commands:
    lint:
      run: pnpm -r run lint
    format:
      run: pnpm --filter server run format:check

pre-push:
  commands:
    tests:
      run: pnpm --filter server run test:coverage
